package main

/*
func readConfig(name string) (err error) {
	if name == "Config.ini"{
		return nil
	}else{
		return errors.New("读取文件错误...")
	}
}


func test()  {
	err := readConfig("1Config.ini")
	if err != nil {
		panic(err)
	}

	fmt.Printf("go on testing")

	//defer + recover   处理和捕获异常
	defer func() {
		// if err := recover() ; err != nil 同下
		err := recover()
		if err != nil{
			fmt.Printf("error is %v",err)
		}
	}()
	num1 := 100
	num2 := 0
	res := num1/num2
	fmt.Printf("%v",res)

}
*/
func main() {
	/*
		num1 := 100
		fmt.Printf("num1 = %T \n num1 = %v\n num1 = %v\n",num1,num1,&num1)
		//new()用来分配内存
		num2 := new(int)
		*num2 = 100
		fmt.Printf("num2 = %T\nnum2 = %v\nnum2 = %v\nnum2 value is %v\n",num2,num2,&num2,*num2)
	*/
}

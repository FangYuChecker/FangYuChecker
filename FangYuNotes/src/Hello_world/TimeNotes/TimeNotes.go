package TimeNotes

func TimeNotes() {
	//now := time.Now()
	/*

		fmt.Printf("time is %v",now)
		fmt.Printf("time is %v\n",now.Year())
		fmt.Printf("time is %v\n",int(now.Month()))
		fmt.Printf("time is %v\n",now.Day())
		fmt.Printf("time is %v\n",now.Hour())
		fmt.Printf("time is %v\n",now.Minute())
		fmt.Printf("time is %v\n",now.Second())
		fmt.Printf("time is %v\n",now.YearDay())
		fmt.Printf("time is %v\n",now.Weekday())

	*/
	//格式化,后面的时间是固定的写法，不允许修改，可以缩短
	//fmt.Printf("time is %v\n",now.Format("2006/01/02 15:04:05"))

	//每隔1s time.Sleep(time.Second)
	//Unix and UnixNano时间戳 随机数
	//fmt.Printf("Unix is %v\nUnixNano is %v\n",now.Unix(),now.UnixNano())
}
